# 15. Valid Palindrome

Difficulty: Easy  
Topics: string, two-pointers

Statement
Given a string `s`, return `true` if it is a palindrome considering only alphanumeric characters and ignoring cases.

Examples
- Input: `s = "A man, a plan, a canal: Panama"`  
  Output: `true`

Constraints
- `1 <= s.length <= 2 * 10^5`
- `s` consists of printable ASCII characters.

Hints (optional)
- Use two pointers moving inward and skip non-alphanumeric characters.

Canonical solution
- `/languages/python/problems/solutions/15-valid-palindrome.py`
