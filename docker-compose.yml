services:
  dev:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: student-interview-prep-dev
    volumes:
      - .:/workspace
    working_dir: /workspace
    stdin_open: true
    tty: true
    command: /bin/bash
    environment:
      - PYTHONPATH=/workspace
